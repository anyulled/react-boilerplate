/**
*
* AddColumnComponent
*
*/

import React, { PropTypes } from 'react';
import { Link } from 'react-router';
import { Button } from 'react-bootstrap';

function AddColumnComponent({  }) {
    return (
        <div>
            <Button>
                Add Column
            </Button>
        </div>
    );
}

AddColumnComponent.propTypes = {
};

export default AddColumnComponent;